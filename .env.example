DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=postgres
DB_PORT=5432
DB_NAME=smartenergy

REDIS_HOST=redis
REDIS_PORT=6379

JWT_SECRET=change-this-to-a-long-random-secret

NODE_ENV=development
PORT=3000

# AI_MOCK=true  → MockAIProvider (no Ollama needed, for development)
# AI_MOCK=false → OllamaAIProvider (requires ollama service)
AI_MOCK=true
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=llama3.1:8b
